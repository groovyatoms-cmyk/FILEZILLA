# Copy to .env for local development. Never commit a real .env file.

# Port the signaling server listens on.
PORT=8787

# Maximum session TTL the server will honor, in milliseconds, regardless of what a client
# requests. Caps how long a pairing session (and its signaling-side record) can stay alive.
SIGNALING_MAX_TTL_MS=600000
